# 2026-09-26

## mini-spring 官网重构设计方案（设计层，未改代码）

**任务**：为 GitHub Pages 站点制定重构设计方案，仅设计层面，要求专业/精致/大气。

**现状基线（已核实）**
- VitePress 1.6.3 + 默认主题 + 单文件 `docs/.vitepress/theme/custom.css`（~600 行）覆写
- 视觉取向：强复刻 Drizzle ORM（近黑表面 + 单一品牌色 #2e63e7 + 细边框 + 小圆角 8px + 紧凑排版）
- base 路径 `/mini-spring/`，GitHub Actions → Pages（`.github/workflows/deploy-docs.yml`，node 22 + npm ci）
- 页面总数 20：`index.md` + `en/index.md` + guide 9 篇 + en/guide 9 篇，双语路由同构
- 侧栏 4 组：前言 / 快速开始 / IoC 容器(4) / 进阶特性(2)；顶部导航仅 3 项
- 资产：架构图 5 组（SVG+PNG 双份，在 `img/` 与 `docs/public/images/`）、logo 2 份、书籍封面 2
- 源码规模：134 个 Java 类（original 36 / iteration 98），测试 2 个（ApiTest、CircleTest）
- iteration 顶层包：aop / beans / context / core / stereotype / utils

**产出**：`design/site-redesign-spec.md`（十章，约 700 行）
- 诊断 7 个问题：信息层级过扁、门户与文档割裂、缺决策型/工具型/信任型内容、首页节奏单调、视觉体系未成文
- 信息架构：两页制 → 四层（门户 / 分流 / 文档 / 支撑），页面 20 → 34
- 新增 7 类页面：两条路径对比、类参考索引、学习路线、目录结构导览、常见报错排查、FAQ、更新日志、关于与致谢、贡献指南
- 首页 6 段 → 8 段（新增「数据条」「代码一瞥」）
- 视觉：收敛为 4 组设计令牌（色彩/字体/间距/圆角描边）；强调色 6 色 → 蓝青紫 3 色循环；引入 Inter + JetBrains Mono；圆角 8→10px；区块留白 40→72px；Hero 标题 54→60px
- 导航 3 → 6 项 + 页脚 4 列矩阵；补齐 9 态交互矩阵（现有仅 hover）
- 响应式 6 断点（640/768/1024/1280/1600）+ WCAG 2.1 AA 基线

**关键发现**：现有 `--ds-text-2` 亮的 `#9aa0a6` 在浅底对比度仅 ~2.8:1，不达 AA 4.5:1，方案中要求加深至 `#666c76`。

**后续**：按 P0 令牌落地 → P1 分流层 → P2 首页 → P3 导航 → P4 支撑层 → P5 响应式 推进。

---

## 线框级别细化（第二轮）

**产出**：`design/wireframes.md`（1452 行 / 87K 字符），17 章 + 附录
- 第 1–12 章：页面级线框，覆盖全部 19 个页面条目（首页 / 快速开始 3 页 / 架构总览 / 对比 / 类参考 / 路线 / 文档页骨架 / FAQ / 更新日志 / 关于 / 贡献 / 404）
- 第 13–15 章：导航结构（顶部导航 6 项含 2 个下拉、侧栏 6 组含折叠与抽屉、页脚 4 列矩阵）
- 第 16–17 章：组件级全状态线框（按钮 / 特性卡 / 提示块 / 代码块 / 表格 / 徽章 / 分段控件 / 折叠面板 / 时间轴）
- 附录：19 页面清单（路由 / 布局 / 内容宽 / 线框章节 / 新增或沿用）+ 12 类组件复用索引

**规模**：196 个线框区块、933 行线框、261 行规格表，宽度全部 82 视觉宽（零对齐异常）

**方法（可复用）**：
- 用 Python 脚本生成 ASCII 线框，`unicodedata.east_asian_width` 判 CJK 双宽，`fit()` 统一补齐/截断到固定视觉宽
- 核心渲染函数：`top/bot/row`（框）、`ruler`（区块分隔）、`grid`（横向卡片）、`table`（盒内表格）、`stack`（纵向堆叠）、`frame`（整体包裹）
- 关键教训：**表格与卡片必须用不同渲染函数**——早期误用横向 grid 渲染 3 列表格，导致行宽膨胀至 1000+ 视觉宽，必须用 `table()` 逐格拼接
- 每部分生成后立即校验行宽恒为 82，再合并，避免错误累积

**计数修正**：方案原写「20 → 34 页面」有误，已核实并统一为 **20 → 38 页面**（19 条目 × 双语）；新增条目实为 9 类而非 7 类。

---

## 重构实施（第三轮，代码落地）

**结果**：构建通过，**40 个 HTML 页面**（19 条目 × 双语 + 404 双语），最终校验全绿。

**校验基线（最终）**
- 链接：40 页 / 1456 个锚点 → **0 失效**
- 运行时：40 页 → 0 console 错误、0 失败请求、0 破图、每页均有 h1 与标题
- 响应式：15 个断点（1440→360）× 40 页 → **0 横向溢出**

**主题基建**（`docs/.vitepress/theme/`）
- `tokens.css` 四组设计令牌（色彩/字体/间距/圆角描边），明暗双份；`components.css` ~2200 行
- 14 个全局组件：`DsHero / DsStatBar / DsFigureGallery / DsClassIndex / DsRoadmap / DsAccordion / DsSegmented / DsCompare / DsCodeGlance / DsDocHead / DsStructure / DsErrorTable / DsFaq / Ds404`
- 数据模块 `data/classes.ts`（127 类，与源码 comm 比对零差异）、`data/errors.ts`、`data/faq.ts`

**踩坑与修复（都是真实缺陷，非风格问题）**
1. **markdown-it HTML block(type 6) 遇空行即终止** → 块内空行会让后续缩进标签渲染成 `<pre><code>`，首页卡片变代码、栅格崩塌。已修 8 文件 58 行，并加 `scripts/check-html-blocks.mjs` 挂到 `prebuild`。
2. **原始 HTML 里的 `href="/xxx"` 不会被 VitePress 补 base** → base=`/mini-spring/` 下直开/新标签页 404。共 **41 处**（15 个 md）+ 组件若干 + 书籍封面 `src` 4 处。
   - 方案：新增 `theme/siteHref.ts`（补 base；未开 `cleanUrls` 时补 `.html`，与 VitePress 自身链接形式一致），全局属性 `$href` 供 markdown 用，组件内 `import { siteHref }`。
   - 另加 `scripts/check-internal-links.mjs` 守卫，同样挂 `prebuild`（`npm run check`）。
3. **`en/index.md` 用 `./en/xxx`** → 从 `/mini-spring/en/` 解析成 `/mini-spring/en/en/xxx`，10 处全挂。改为 `./xxx`。
4. **VitePress 的 404 由内置 `NotFound.vue` 渲染**，自研 `Ds404` 从未生效。正解：默认主题 `Layout` 会把 `#not-found` 插槽透传给 `VPContent`，在该插槽挂 `<Ds404 />` 即可（插槽有内容就不再回退内置组件）。另补 `en/404.md`，因为语言切换会指向 `en/404.html`。
5. **`style="grid-template-columns: repeat(5,1fr)"` 内联样式压制所有媒体查询** → 贡献页 5 步流程在窄屏溢出。改为 `.ds-steps--5` 类 + 断点。
6. **`1fr` 的隐式最小宽度是 min-content** → 文档页内容区仅 ~620px 时三列卡片撑破栅格。改用 `repeat(N, minmax(0, 1fr))`。
7. 其他：`.ds-changes code` 长类名折行、`.ds-segmented` 窄屏等分整宽、英文站名在 768–900px 撑破导航（该区间隐藏站名文字只留 logo）、`Compare.vue` 的 scoped 样式挪到全局（markdown 手写表格吃不到 scoped）。

**环境坑**
- Git Bash(MSYS) 会把命令行里的 `/xxx` 参数改写成 Windows 路径 —— 传 URL 路径给 node 脚本必须加 `MSYS_NO_PATHCONV=1`。
- `vitepress dev` 会锁住 `dist/`，会让下一次 `build` 的删除步骤失败；构建前务必先停掉。
- 不能用 Bash 调 `powershell.exe`（安全策略拦截），改用 PowerShell 工具。

**验证脚本（临时，在 C:/tmp）**：`brokenlinks.mjs`（锚点解析+文件存在性）、`verify.mjs`（运行时健康 + 15 断点溢出）、`shot.mjs`（双主题截图）



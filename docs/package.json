{
  "name": "mini-spring-docs",
  "private": true,
  "scripts": {
    "dev": "vitepress dev",
    "check:blocks": "node scripts/check-html-blocks.mjs",
    "check:links": "node scripts/check-internal-links.mjs",
    "check": "npm run check:blocks && npm run check:links",
    "prebuild": "npm run check",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0"
  }
}
